# 更新日志

所有 notable 变更都会记录在此文件中。

本格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

---

## [1.0.6] - 2026-04-15

### 🐛 修复
- 修复共享同步链路仍残留 legacy `menu_items.weight` 字段的问题，避免个人权重继续被当成共享数据传播。
- 修复菜谱详情、编辑页和历史恢复对权重的读取不一致问题；现在权重只来自本地 `personalWeights`。
- 修复旧服务端变更快照可能把 `weight` 暴露回前端并在恢复历史版本时重新写回菜单记录的问题。

### ✅ 验证
- 新增同步清洗、历史快照清洗与 Dexie 迁移回归测试。
- `npm run test:run`
- `npx tsc --noEmit`
- `npm run build`

---

## [1.0.5] - 2026-04-15

### 🐛 修复
- 修复导入旧备份时遗失历史个人权重的问题；现在会自动把 legacy `menuItems.weight` 迁移到 `personalWeights`。
- 修复菜单编辑弹窗关闭后未保存权重草稿丢失的问题；重新打开时会优先恢复草稿中的权重值。

### ✅ 验证
- 新增旧备份导入兼容测试与菜单表单权重草稿测试。
- `npm run test:run`
- `npx tsc --noEmit`
- `npm run build`

---

## [1.0.4] - 2026-04-14

### 🐛 修复
- 将个人权重迁移为独立存储表，避免个人抽选偏好继续和共享菜单基础数据耦合。
- 修复抽选逻辑仍直接读取菜单权重的问题，确保个人权重在单抽和组合抽选中都能正确生效。
- 修复删除菜单项时遗漏清理个人权重的问题，避免产生孤立数据。
- 导入导出流程补充个人权重数据，减少换设备或恢复备份时的偏好丢失。

### 🧪 测试
- 补充数据库、导入导出和抽选逻辑测试覆盖个人权重场景。
- `npm run test:run`
- `npm run build`

---

## [1.0.3] - 2026-04-14

### 🐛 修复
- 收口菜单页、标签页、模板页后台同步轮询的未处理 Promise，避免同步接口异常时持续向控制台抛出未捕获错误。
- 修复变更记录页在加载失败时一直停留在“加载中”的问题，改为显示明确的失败提示。
- 修复菜单详情弹窗历史版本加载失败时卡住的问题，并在失败时给出内联错误提示。
- 收口菜单编辑弹窗的变更记录探测异常；同步后端不可用时不再影响编辑流程。

### ✅ 验证
- `npm run test:run`
- `npm run build`

---

## [1.0.2] - 2026-04-14

### 🐛 修复
- 修复生产环境未注入 `NEXT_PUBLIC_API_BASE_URL` 时，前端错误请求 `/sync/*`、`/spaces/*`、`/auth/*` 路径导致同步与加入空间接口返回 404 的问题。
- 新增统一的前端 API base 处理逻辑；同域部署默认走 `/api`，显式配置时才覆盖为自定义 API 域名。
- 改进同步 404 错误提示，直接提示检查 `NEXT_PUBLIC_API_BASE_URL` 并在修改后重新执行构建。

### ✅ 验证
- 新增和扩展 Vitest 覆盖默认 `/api` 与自定义 API base 两种场景。
- 当前单元测试已通过，生产构建已通过。

---

## [1.0.1] - 2026-04-13

### 🐛 修复
- 修复手机端编辑菜单弹窗中材料清单删除按钮与圆角边框重叠的问题。
- 修复手机端步骤操作按钮在窄屏下排列错位的问题。
- 为弹窗底部增加安全区适配（`safe-area-inset-bottom`），避免取消/保存按钮与底部导航栏重叠。
- 提升弹窗层级，确保打开时正确覆盖底部固定导航栏。

---

## [1.0.0] - 2025-04-13

### 🎉 初始版本发布

> 从今天起，不再纠结"今天吃什么"。

### ✨ 功能
- **随机抽选**：支持"自己做"、"叫外卖"和自定义组合模板抽选。
- **菜单管理**：添加 / 编辑 / 删除菜品，支持标签分类、材料清单、烹饪步骤和图片上传。
- **标签体系**：自定义标签（菜系、类别、自定义），支持多标签筛选与搜索。
- **组合模板**：按规则批量抽取，如"1 荤 + 1 素 + 1 汤"。
- **历史记录**：自动记录每次抽选结果，随时回看"今天吃了什么"。
- **忌口管理**：标记忌口菜品，抽选时自动规避，支持一键查看与取消。
- **多设备同步**：通过邀请码加入同一共享空间，多台设备实时同步菜单数据，匿名即可使用。
- **PWA 支持**：可安装到手机桌面，离线可用，已针对移动端进行交互和布局优化。

### 🛠️ 技术
- 前端：Next.js 14 (App Router) + React 18 + TypeScript + Tailwind CSS
- 后端：Next.js API Routes + better-sqlite3（单文件 SQLite）
- 客户端存储：Dexie.js (IndexedDB)
- 测试：Vitest + jsdom，47 项单元测试全部通过

### 🐛 修复
- 修复种子数据缺失 `spaceId` 导致的编辑后无法同步问题。
- 修复移动端弹窗按钮和标签区域可能溢出的问题。
- 修复菜单页 React 异步状态更新警告。
